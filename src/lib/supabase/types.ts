/**
 * Placeholder until the schema is generated from the live database.
 *
 * After running supabase/migrations/0001_init.sql, regenerate this file with:
 *   npx supabase login
 *   npx supabase gen types typescript --project-id <your-project-ref> > src/lib/supabase/types.ts
 *
 * The project ref is the subdomain in NEXT_PUBLIC_SUPABASE_URL.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any
